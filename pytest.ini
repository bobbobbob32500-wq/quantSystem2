[pytest]
testpaths = tests
addopts = -ra
markers =
    smoke: Fast core regression tests for daily development and release gates.
    slow: Data-heavy or longer-running regression tests.
    nightly: Full regression tests intended for overnight or scheduled validation.
