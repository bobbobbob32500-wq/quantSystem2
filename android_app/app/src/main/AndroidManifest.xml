<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuantSystem"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">
        <profileable android:shell="true" tools:targetApi="q" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.QuantSystem.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- 优化测试Activity -->
        <activity
            android:name=".ui.screen.OptimizationTestActivity"
            android:exported="true"
            android:theme="@style/Theme.QuantSystem"
            android:label="优化测试"
            android:screenOrientation="portrait" />
        
        <!-- 优化报告Activity -->
        <activity
            android:name=".ui.screen.OptimizationReportActivity"
            android:exported="true"
            android:theme="@style/Theme.QuantSystem"
            android:label="优化报告"
            android:screenOrientation="portrait" />
        
        <!-- 优化启动器Activity -->
        <activity
            android:name=".core.test.OptimizationLauncherActivity"
            android:exported="true"
            android:theme="@style/Theme.QuantSystem"
            android:label="优化启动器"
            android:screenOrientation="portrait" />
    </application>

</manifest>
