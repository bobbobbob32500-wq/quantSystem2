# 系统优化实施指南

**完成日期**: 2026年3月30日  
**阶段**: 第一阶段 - 基础优化完成

---

## 已完成的优化

### 1. ✅ 代码规范框架 (`src/core/code_standards.py`)

创建了统一的代码规范工具库，包含：

#### 异常处理装饰器
```python
@handle_exceptions(default_return=[])
def risky_function():
    pass
```

#### 性能监控装饰器
```python
@monitor_performance(threshold_ms=5000)
def slow_function():
    pass
```

#### 缓存装饰器
```python
@cached(ttl_seconds=3600)
def expensive_function(param):
    pass
```

#### 并行执行工具
```python
results = ParallelExecutor.map_threads(process_func, items, max_workers=8)
```

#### 配置验证器
```python
ConfigValidator.validate_range(value, 0, 100, "score")
ConfigValidator.validate_positive(value, "count")
ConfigValidator.validate_choice(value, ["a", "b"], "option")
```

### 2. ✅ 数据库优化 (`tools/db_optimizer.py`)

创建了数据库优化脚本，包含：

- **创建索引**: 为关键表添加索引
  - stock_daily (ts_code, trade_date)
  - index_daily (ts_code, trade_date)
  - factor_values (symbol, calc_date)
  - signal_history (ts_code, signal_date)
  - trade_log (ts_code, trade_date)

- **表分析**: 优化查询计划
- **数据库清理**: 回收空间

**使用方法**:
```bash
python tools/db_optimizer.py
```

### 3. ✅ 测试框架 (`tests/conftest.py`)

建立了 pytest 测试框架，包含：

- **Fixtures**: config, db, sample_stock_data, sample_market_data 等
- **标记**: @pytest.mark.unit, @pytest.mark.integration, @pytest.mark.slow
- **临时目录**: temp_dir fixture
- **缓存清理**: cleanup_cache fixture

**使用方法**:
```bash
# 运行所有测试
pytest tests/ -v

# 运行单元测试
pytest tests/ -v -m unit

# 运行集成测试
pytest tests/ -v -m integration

# 生成覆盖率报告
pytest tests/ --cov=src --cov-report=html
```

### 4. ✅ 单元测试示例 (`tests/test_code_standards.py`)

创建了代码规范工具的单元测试，包含：

- 异常处理测试
- 性能监控测试
- 缓存测试
- 并行执行测试
- 配置验证测试

---

## 下一步行动计划

### 第二阶段：版本统一（1周）

#### 任务1：合并选股版本
```
删除：
- src/modules/enhanced_hybrid_system.py
- src/modules/optimized_buy_signals.py
- src/modules/wide_entry_strict_selection.py

保留：
- src/modules/stock_selector.py (基础版)
- 新增 src/modules/stock_selector_v2.py (增强版)

使用特性开关：
config.get('stock_selection.strategy_profile') 
  -> 'legacy' (基础版)
  -> 'enhanced' (增强版)
```

#### 任务2：统一回测引擎
```
删除：
- src/modules/backtest/improved_backtest_engine.py
- src/modules/backtest/event_backtest_engine.py

保留：
- src/modules/backtest/strategy_backtest_engine.py (主版本)
```

### 第三阶段：代码规范应用（1周）

#### 任务1：应用装饰器到关键函数
```python
# stock_selector.py
@handle_exceptions(default_return=[])
@monitor_performance(threshold_ms=5000)
@cached(ttl_seconds=3600)
def run_selection(self, end_date: Optional[str] = None) -> List[Dict]:
    """执行选股"""
    pass

# risk_controller.py
@handle_exceptions(default_return={})
@monitor_performance(threshold_ms=2000)
def run_risk_check(self, holds: List[Dict]) -> Dict:
    """执行风控检测"""
    pass
```

#### 任务2：添加类型提示
```python
# 改进前
def calculate_score(data):
    return data * weight

# 改进后
def calculate_score(self, data: pd.DataFrame, weight: float) -> float:
    """计算评分"""
    return (data * weight).sum()
```

#### 任务3：完善异常处理
```python
# 改进前
try:
    result = self.stock_selector.run_selection()
except Exception as e:
    print(f"选股失败: {e}")

# 改进后
try:
    result = self.stock_selector.run_selection()
except StockSelectionException as e:
    logger.error(f"选股失败: {e}", exc_info=True)
    raise
except Exception as e:
    logger.critical(f"未预期的错误: {e}", exc_info=True)
    raise SystemException(f"系统错误: {e}") from e
```

### 第四阶段：性能优化（2周）

#### 任务1：应用缓存
```python
# 市场分析结果缓存
@cached(ttl_seconds=300)  # 5分钟缓存
def analyze_market(self, end_date: str = None) -> Dict:
    pass

# 股票基础信息缓存
@cached(ttl_seconds=86400)  # 1天缓存
def get_stock_basic_info(self, ts_code: str) -> Dict:
    pass

# 因子计算缓存
@cached(ttl_seconds=3600)  # 1小时缓存
def calculate_factors(self, data: pd.DataFrame) -> pd.DataFrame:
    pass
```

#### 任务2：应用并行处理
```python
# 并行选股
def run_selection(self, end_date: str = None) -> List[Dict]:
    stocks = self._get_stock_universe()
    
    # 并行计算因子
    factors_list = ParallelExecutor.map_threads(
        self._calculate_stock_factors,
        stocks,
        max_workers=8
    )
    
    results = self._merge_and_sort(factors_list)
    return results
```

#### 任务3：运行数据库优化
```bash
python tools/db_optimizer.py
```

### 第五阶段：测试覆盖（2周）

#### 任务1：编写单元测试
```bash
# 为关键模块编写单元测试
tests/test_stock_selector_unit.py
tests/test_risk_controller_unit.py
tests/test_market_analyzer_unit.py
tests/test_position_controller_unit.py
```

#### 任务2：运行测试并生成报告
```bash
pytest tests/ -v --cov=src --cov-report=html
# 目标：覆盖率 >80%
```

#### 任务3：集成测试
```bash
pytest tests/ -v -m integration
```

---

## 预期改进效果

### 性能改进
| 指标 | 当前 | 目标 | 改进 |
|------|------|------|------|
| 选股耗时 | 30-60s | <5s | 10倍 |
| 回测速度 | 5-10s/股 | <1s/股 | 10倍 |
| 内存占用 | 500MB-1GB | <200MB | 3倍 |
| 数据库查询 | 无索引 | 有索引 | 5倍 |

### 质量改进
| 指标 | 当前 | 目标 |
|------|------|------|
| 测试覆盖率 | <50% | >80% |
| 代码重复度 | 高 | 低 |
| 文档完整性 | 不足 | 完善 |
| 代码规范符合度 | 70% | 95% |

---

## 使用新工具的示例

### 示例1：使用缓存优化选股
```python
from src.core.code_standards import cached, monitor_performance, handle_exceptions

class StockSelector:
    @handle_exceptions(default_return=[])
    @monitor_performance(threshold_ms=5000)
    @cached(ttl_seconds=3600)
    def run_selection(self, end_date: Optional[str] = None) -> List[Dict]:
        """执行选股"""
        # 选股逻辑...
        return results
```

### 示例2：使用并行处理加速回测
```python
from src.core.code_standards import ParallelExecutor

class BacktestEngine:
    def run_backtest_parallel(self, stocks: List[str]) -> Dict:
        """并行回测"""
        results = ParallelExecutor.map_threads(
            self._backtest_single_stock,
            stocks,
            max_workers=8
        )
        return self._merge_results(results)
```

### 示例3：使用配置验证
```python
from src.core.code_standards import ConfigValidator

class RiskController:
    def __init__(self, config):
        # 验证配置参数
        ConfigValidator.validate_range(
            config.get('risk_control.volume_high_ratio'),
            0, 1, 'volume_high_ratio'
        )
        ConfigValidator.validate_positive(
            config.get('risk_control.volume_high_days'),
            'volume_high_days'
        )
```

---

## 检查清单

### 立即可做（今天）
- [x] 创建代码规范框架
- [x] 创建数据库优化脚本
- [x] 建立测试框架
- [x] 编写示例测试

### 本周可做
- [ ] 运行数据库优化
- [ ] 运行现有测试
- [ ] 开始版本统一
- [ ] 应用装饰器到关键函数

### 本月可做
- [ ] 完成版本统一
- [ ] 应用所有代码规范
- [ ] 提升测试覆盖率 >80%
- [ ] 性能优化

### 本季度可做
- [ ] 完成所有P0优化
- [ ] 性能提升10倍
- [ ] 文档完善

---

## 快速开始

### 1. 运行数据库优化
```bash
cd d:\HuaweiAI\quantSystem2
python tools/db_optimizer.py
```

### 2. 运行测试
```bash
pytest tests/test_code_standards.py -v
```

### 3. 生成覆盖率报告
```bash
pytest tests/ --cov=src --cov-report=html
# 打开 htmlcov/index.html 查看报告
```

### 4. 在代码中使用新工具
```python
from src.core.code_standards import (
    handle_exceptions,
    monitor_performance,
    cached,
    ParallelExecutor,
    ConfigValidator
)

# 在你的函数上应用装饰器
@handle_exceptions(default_return=[])
@monitor_performance(threshold_ms=5000)
@cached(ttl_seconds=3600)
def your_function():
    pass
```

---

## 文档参考

- **代码规范**: `src/core/code_standards.py`
- **数据库优化**: `tools/db_optimizer.py`
- **测试框架**: `tests/conftest.py`
- **示例测试**: `tests/test_code_standards.py`

---

**下一步**: 开始第二阶段 - 版本统一与代码规范应用
